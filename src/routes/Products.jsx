import data from "../data/Api"

const productData = data

const Products = () => (
    <section>
        <div className="start wrapper">
            <h4>Dinmamma</h4>
        </div>
    </section>
)
export default Products